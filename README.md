# 🎮 GuessingGameBot 🎮

## Описание

**GuessingGameBot** — это Telegram-бот для игры в угадывание чисел. Пользователи могут угадывать случайные числа в диапазоне, который они задают при запуске игры. Бот поддерживает команды для старта, угадывания и остановки игры.

## Основные функции

- **/start [лимит]** — Начинает новую игру. Пользователь задает диапазон чисел (от 100 до 1000, кратный 100), и бот генерирует 15 случайных чисел в этом диапазоне.
- **/stop** — Останавливает активную игру и показывает оставшиеся неугаданные числа.
- **Угадывание числа** — Пользователь вводит число, и бот сообщает, угадал он или нет.

## Как играть

1. Начни игру, введя команду `/start [лимит]`, где лимит — это максимальное число в диапазоне (100, 200, ..., 1000). Например:
   ```
   /start 300
   ```
   Бот выберет 15 случайных чисел от 1 до 299, и ты начнешь угадывать.

2. Угадай число, вводя его в чат. Бот ответит, угадал ли ты, или это число уже было угадано.

3. Если все числа угаданы — игра завершится, и ты получишь сообщение об этом! 🎉

4. В любой момент можно остановить игру командой `/stop`, чтобы увидеть неугаданные числа.

## Примеры использования

- **Начало игры**:
  ```
  /start 500
  ```
  Ответ бота:
  ```
  🎮 Игра началась! Попробуйте угадать числа от 1 до 499.
  ```

- **Попытка угадать число**:
  ```
  25
  ```
  Возможный ответ бота:
  ```
  🎉 Правильно! @user угадал(а) число 25!
  ```

- **Остановка игры**:
  ```
  /stop
  ```
  Ответ бота:
  ```
  🛑 Игра остановлена. Загаданные числа были: [1, 57, 89, ...]
  ```

## Требования

- **Java 8+**
- **Maven**
- **Telegram Bot API**

## Установка

1. Клонируй репозиторий:
   ```bash
   git clone https://github.com/kitaezov/GuessingGameBot.git
   cd GuessingGameBot
   ```

2. Собери проект:
   ```bash
   mvn package
   ```

3. Создай Telegram-бота с помощью [@BotFather](https://t.me/botfather), получи токен и добавь его в код, а так же добавь юзернейм!:
   ```java
   @Override
   public String getBotToken() {
       return "Токен бота";
   }


   @Override
    public String getBotUsername() {
      return "Название бота, без @";
    }
